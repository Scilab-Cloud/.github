# .github
Scilab Cloud keeps scripts you own in one named numerical desk so plots stay easy to reverse on a host you already operate. Scilab keeps the console honest, so Scilab Software stays a file you can still restore later without extra noise on that duty folder.
